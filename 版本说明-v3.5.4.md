# Voice Input 3.5.4 版本说明

## 通用文字写入安全

- 彻底删除合成 Unicode 键盘事件和虚拟 A 键载体，不存在任何开关可以重新启用。
- 所有 App 统一使用 macOS 辅助功能文本写入，并在写入后读回校验。
- 目标输入框不允许写入时，只保留本机恢复记录，不发送字符键、剪贴板或回车键。

## 状态栏与设置

- 删除状态栏控制器的录音回调；左键和右键都只能打开并置前设置窗口。
- 录音只能由用户选定的“按住说话”快捷键触发。
- 设置页收敛为“设置”与“千问”两个标签，只展示快捷键、标点、API Key、地域/语言和权限。
- 隐藏实验性口语清理、API Host/Workspace、个性化上下文、纠错表和诊断细节；保留已有本机数据，升级时不删除。
- 快捷键后端根据按键自动选择，不再向用户暴露 backend/global 概念。口语清理固定关闭。

## 回滚

3.5.3 安装包仍保留在 `outputs/`。无 Developer ID 时 App 使用 ad-hoc 签名，更新 App 本体后 macOS 可能要求重新确认一次辅助功能权限。
