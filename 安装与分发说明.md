# VoxType：安装、分发与继续开发

首次使用请先阅读带截图的 [快速开始与 API 配置](./docs/快速开始与API配置.md)。本文重点说明安装包分发、源码开发和升级。

## 普通用户：无需 Python

### 选择安装包

- `Voice-Input-v3.6.4-macOS-arm64-internal.dmg`：打开后拖入“应用程序”。
- `Voice-Input-v3.6.4-macOS-arm64-internal.pkg`：双击并按安装器提示安装。
- `.zip`：DMG 创建失败时的等价备用 App 包。

这些包面向 Apple 芯片、macOS 13 或更高版本，已经内置 Python 和依赖。由于没有 Apple Developer ID，App 本体采用 ad-hoc 签名，PKG 安装器容器不签名，二者都不能公证，只适合可信的小范围传播；公司管控设备仍可能禁止未知来源 App、辅助功能或外部 API。

### 首次启动

1. 将 `VoxType` 放入“应用程序”并打开。
2. 若被 macOS 拦截，进入“系统设置 → 隐私与安全性”，在安全提示处选择“仍要打开”；也可以在 Finder 中右键 App 选择“打开”。
3. 首次打开会出现权限引导页。页面会核对当前运行版本、App 路径、PID 和可执行文件指纹，再检测麦克风、辅助功能及输入监控权限；按“授权”并完成 macOS 系统确认即可。
4. 在“引擎”页选择千问或豆包并填写自己的凭证，点击“保存并重启”。
5. 默认按住键盘右侧的 Option 说话，松开后最终文字写到原光标处。默认“只读监听”不会拦截或修改原按键事件。

点击菜单栏波形图标只会打开设置，不会启动录音；也可在“应用程序”中再次打开 VoxType，或使用源码版的 `open-settings.command`。

### 获取千问 API Key

本工具需要阿里云百炼 API Key，不是阿里云 AccessKey ID/Secret，也不是 API Key 列表中的数字 ID。

1. 登录并打开[百炼 API Key 页面](https://bailian.console.aliyun.com/?tab=model#/api-key)。
2. 在右上角确认地域；本工具默认使用华北 2（北京）。
3. 点击“创建 API Key”，建议选择默认业务空间。权限可选“全部”；需要最小权限时，确保允许 `qwen-audio-3.0-asr-flash-streaming`。
4. 创建成功后立刻保存完整 API Key 和 API Host。新版 `sk-ws-` Key 的明文只展示一次，关闭后需重置或重建。
5. 回到设置页粘贴 API Key 和 OpenAI compatible API 地址。两者必须与地域、业务空间或套餐匹配。

阿里云官方说明：[获取 API Key](https://help.aliyun.com/zh/model-studio/get-api-key/)；实时 ASR 文档：[Qwen Audio 3.0 ASR Streaming](https://platform.qianwenai.com/docs/developer-guides/speech/asr-realtime)。每位接收者应使用自己的 Key，不要把凭证放入聊天、源码或安装包。

### 获取豆包凭证

1. 打开[火山引擎语音应用](https://console.volcengine.com/speech/app)。
2. 创建或选择应用，保存 App ID 和 Access Token。
3. 为应用开通“豆包流式语音识别模型 2.0 小时版”。
4. 在 VoxType 的“引擎”页选择豆包并填写两项凭证。

每位接收者同样应使用自己的凭证。App 不会把千问个性化上下文发送给豆包。

### 使用与排错

- 胶囊会实时显示当前引擎的累计预览；松开后目标输入框只接收一次最终文字。
- 按住录音时按 Esc 可取消，本轮迟到的网络结果也不会写入。
- 录音中若切换到别的窗口，程序不会把结果打到新窗口；文字会出现在“恢复与诊断”。
- 默认不保存录音、不启用口语清理；最终文字短暂进入剪贴板完成兼容粘贴，随后恢复原内容。
- “口语清理”建议先用“仅去除嗯、呃、啊”；去重复模式属于实验性选项。
- 自动观察人工修改的纠错学习仍为实验代码，正式安装包默认禁用，也不在普通用户设置页中展示。
- 快捷键突然失效时，先确认按的是右 Option，再检查密码框、锁屏或 Secure Input。
- 公司安全软件若拦截右 Option/Fn，在设置中改用 `⌃⌥Space`。程序不会尝试绕过企业安全策略。
- 更方便的企业兼容方案是选择“系统注册组合键”，按住 `Control + Option + Space` 说话；它只注册这一组系统快捷键，不读取其他键，也不需要输入监控权限。
- 只有快捷键无反应时，检查“系统设置 → 隐私与安全性 → 输入监控”是否允许 VoxType；只勾选“辅助功能”并不足以接收全局按键。
- “只读监听”只能观察选定快捷键，不能吞键；v3.5.1 已移除旧的按键拦截依赖与实现。

运行数据位于 `~/Library/Application Support/VoxType`。更新 App 不会覆盖配置和学习数据。由于内测 App 使用 ad-hoc 签名，每次重建都会改变代码签名身份；覆盖升级后若右 Option/Fn 失效，需要在系统“辅助功能”和“输入监控”中删除旧条目、重新添加当前 `/Applications/VoxType.app`，再重启 App。

## 源码安装

源码包适合开发者，或无法运行内测 App 的电脑：

1. 解压后放到固定目录。
2. 双击 `install.command`。
3. 脚本会创建 `.venv`、安装依赖、编译 SwiftUI 设置页并注册 LaunchAgent。
4. 在设置页填写自己的 Key 并授权。

源码版设置和数据位于项目目录。卸载时双击 `uninstall.command`；它只移除 LaunchAgent，不删除配置。

## 继续开发

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp env.example .env
./build-native-settings.command
python -m compileall -q main.py src settings_ui.py
python -m unittest discover -s test -p 'test_*.py' -q
```

修改 Python 后，源码安装版执行 `launchctl kickstart -k gui/$(id -u)/com.whisper-input-next` 即可加载新代码；修改 Swift 设置页后先运行 `./build-native-settings.command`。内测 `.app` 是冻结产物，修改源码后必须重新构建并替换 App。

## 分发与隐私

运行 `./package-source.command` 生成源码 tar.gz。脚本会排除并审计以下本机数据：

- `.env` 和所有 API Key
- `personal_context.txt`
- `corrections.json`、`recovery.json`、`diagnostics.json`
- `audio_archive/`、`logs/`、`.venv/`
- Git、构建和输出目录

运行 `./build-internal-dmg.command` 生成 ad-hoc 签名的 App、DMG、未签名 PKG 容器、ZIP 和 SHA-256。构建用的 `env.example` 不含 Key，真实 `.env` 不会进入 App。

详细改动见 [版本说明-v3.6.4.md](./版本说明-v3.6.4.md) 和 [版本说明-v3.6.3.md](./版本说明-v3.6.3.md)。

团队继续开发、创建分支、提交代码评审和发布制品的流程见 [CONTRIBUTING.md](./CONTRIBUTING.md)。隐私边界与泄漏处置见 [SECURITY.md](./SECURITY.md)。

公开分发仍建议申请 Apple Developer ID，完成 hardened runtime、公证和 stapling；否则无法消除首次打开的 Gatekeeper 提示。
