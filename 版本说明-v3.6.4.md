# Voice Input v3.6.4

## 当前 App 身份可验证的权限引导

- 首次启动新增原生 macOS 权限引导页，集中展示麦克风、辅助功能和输入监控状态。
- 权限状态由正在运行的 Voice Input 主进程直接读取，不由设置辅助进程代查，也不按系统列表中的同名条目猜测。
- 页面显示当前版本、Bundle ID、实际 App 路径、PID 和主可执行文件指纹；状态文件还会校验 PID、版本与路径全部匹配后才显示“已确认当前运行版本”。
- 因此，旧版或备份 App 即使同名且曾经获得授权，也不会让当前版本误显示为已授权。
- 每项权限提供“授权”按钮：首次申请会触发 macOS 系统弹窗；已经拒绝过时会打开对应的系统设置页。macOS 仍要求用户亲自确认，应用不会绕过系统安全机制。
- “检查并授权”可以一次触发当前缺失权限的系统申请；系统注册组合键会明确显示无需输入监控。

## 隐私边界

权限检查只写入本机 `permissions.json`，内容仅包含布尔状态、版本、路径、PID 和短指纹，不包含录音、识别文字、按键内容或 API Key。
