# VoxType v0.1.0

首个公开版本。基于 Voice Input Next v3.7.x 重命名并开源。

## 重命名

- 项目从 "Voice Input Next" 重命名为 **VoxType**
- bundle id 从 `com.voiceinputnext.qwen` 改为 `com.voxtype.dev`
- 版权链保留 ErlichLiu (Whisper-Input) 和 Mor-Li (Whisper-Input-Next),新增 Wang Jincheng (VoxType)
- 用户数据目录 `~/Library/Application Support/Voice Input Next/` 不动,老用户配置自动继承

## 新增能力

- **盲 Cmd+V 兼容路径**:微信 4.x、VS Code 等 AX 封闭应用现在也能完成输入。Voice Input 拿不到目标 App 的辅助功能锚点时,自动改为把识别结果写入临时剪贴板并发 `⌘V` 到当前最前窗口,而不是放弃
- **浮窗默认固定位置**:主屏底部居中、距底部 0pt(紧贴 Dock 上沿)。在不同 App 间行为一致,不再跟鼠标/光标漂移
- 设环境变量 `VOICE_INPUT_FLOATING_FOLLOW_CARET=1` 可恢复跟随光标的旧行为
- 状态栏图标改用打包的高质量单色 PNG(template image),替代 SF Symbol —— 后者在 macOS 26 Tahoe 上会渲染成空白

## 改进

- `_get_caret_position` 加 sanity check 和三级 fallback;Tahoe 上 Electron 返回的错位 `AXBoundsForRange` 不再被采纳
- `_position_fixed` 是新增的稳定锚点逻辑

## 修复

- `build-internal-dmg.command` 漏 `mkdir build/`,在干净环境(新机器 / CI)上构建会挂 —— 补上

## 验证

- 全量单元测试通过(134 项,2 项 subtests)
- 包含新增覆盖:
  - 浮窗三级 fallback 的 sanity check 和坐标转换
  - 盲 Cmd+V 路径(成功 / 失败 / 异常 / 焦点丢失 / 换行处理)
  - 固定浮窗位置在不同主屏尺寸和原点下的几何

## 已知边界

- macOS 26 Tahoe 上 Electron 应用 AX 仍然不可靠(走盲 Cmd+V)
- 微信 4.x 完全关闭 AX(走盲 Cmd+V)
- 不实现 marked text 下划线(刻意取舍,见 README)
