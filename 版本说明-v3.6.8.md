# Voice Input v3.6.8

## Electron / Chromium 输入架构修复

- Qoder、VS Code、Codex 等 Electron 应用不再先通过 AXValue 写入视觉文字，也不再依赖“空格后回删”来提交编辑状态。
- 改为向当前登录会话发送一次 Unicode 键盘文字事件，让 Chromium 自己更新输入框的 JavaScript 编辑模型。
- 事件源显式使用 `kCGEventSourceStateCombinedSessionState`，投递到 `kCGSessionEventTap`；这比私有事件源更符合普通登录会话 App 的用途。
- Unicode 只附加在 key-down 事件上，避免 Chromium 对 key-up 重复处理。
- 使用 Space 作为无害载体键，不再使用字母 `A`，因此不会再因 Unicode 被忽略而输出 `a`。
- 提交后必须精确验证完整文本和光标位置。若目标应用只接收了载体空格，程序会立即恢复原内容并把识别结果留在恢复记录中，不把失败误报为成功。
- 继续识别 Qoder 的“追加需求或提问”等视觉占位文本，避免把灰色提示拼进语音结果。

## 安全边界

- 不使用剪贴板。
- 不发送 Return，不会自动提交聊天消息。
- Electron 输入失败时不再降级为可能产生“看得见但不能编辑”的 AX 视觉写入。
- 每次写入前仍校验录音开始时捕获的输入框处于焦点，防止文字进入其他窗口。

## 验证说明

自动化测试覆盖正常 Unicode 写入、Qoder/VS Code 路由、Unicode 被忽略后的载体清理、失焦保护及重复最终回调。由于该问题主要出现在部分 macOS 26 + Electron 组合上，仍需在受影响电脑的真实 Qoder 输入框完成最终兼容性验证。
