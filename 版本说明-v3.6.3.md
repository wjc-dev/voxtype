# Voice Input 3.6.3

本版将千问实时识别从 `qwen3-asr-flash-realtime` 升级为 2026 年 7 月发布的
`qwen-audio-3.0-asr-flash-streaming`。

## 主要变化

- 改用 DashScope `/inference` 双工任务协议；麦克风 PCM 以二进制帧实时发送。
- 保留流式胶囊预览，并正确累计服务端已经完成的多个句段，避免只保留最后一句。
- 用户反复纠正过的目标词会作为即时热词发送，默认权重为 4。
- 用户词汇、近期上下文与纠错上下文继续生效；高频纠错优先进入模型的 400 字上下文窗口。
- 中文优先模式同时提示中文和英文，避免 `LightGBM`、`trade-off` 等中英混合术语退化。
- 旧百炼 API Key 可以继续使用；Key 必须拥有 `qwen-audio-3.0-asr-flash-streaming` 权限。

## 升级说明

安装后在“引擎”中选择“千问”，保留原有 API Key 和 API Host，点击“保存并重启”即可。
设置页会显示当前模型为 `Qwen Audio 3.0 ASR Streaming`。
