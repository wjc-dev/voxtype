# Voice Input v3.7.0

## 跨应用兼容粘贴

- 最终文字改为标准 `Command+V` 粘贴，让 Qoder、VS Code、Codex、浏览器等 Chromium/Electron 输入框自行完成真实编辑状态提交。
- 粘贴前在内存中保存原剪贴板所有 item 及其全部可读取格式，粘贴完成后恢复，而不是只恢复纯文本。
- 临时语音文字标记为 `org.nspasteboard.TransientType`、`ConcealedType` 和 `AutoGeneratedType`，请求兼容的剪贴板历史工具不要记录。
- 恢复内容标记为 restored/transient，降低原内容在历史工具中重复出现的概率。
- 若粘贴期间用户或其他 App 复制了新内容，程序不会覆盖它；用户的新剪贴板优先。
- 不发送 Return，不会自动提交聊天消息。

## 已知边界

macOS 不会强制第三方剪贴板管理器遵守 transient/concealed 标记。Alfred、Maccy、Raycast 等支持相应隐私机制；完全忽略标记的工具仍可能保存临时文字，Voice Input 无权删除其他 App 的历史数据库。
