# Voice Input 3.5.5 版本说明

## ChatGPT/Codex 输入框修复

- 所有辅助功能快照都先归一化视觉占位文字，不再只在 AXValue 写入阶段临时处理。
- 优先使用 `AXPlaceholderValue`、标题/描述对应、真实字符数和可访问文本范围判断空编辑器，适配不同语言的占位提示。
- 兼容 ChatGPT 中文界面未按标准属性暴露的“随心输入”，不会再将它写入正文。
- 同一输入框在 10 秒内的相邻两轮语音若高度重叠，只插入新增后缀；完全相同时不再插入第二次。
- 保留单轮最终回调幂等门禁，并新增占位与跨轮重复的回归测试。

## 回滚

3.5.4 安装包仍保留在 `outputs/`。更新 ad-hoc 签名 App 后，macOS 可能要求重新确认一次辅助功能权限。
