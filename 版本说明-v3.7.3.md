# Voice Input v3.7.3

## 微信粘贴兼容性修复

- 临时剪贴板写入后增加短暂同步等待，避免自定义输入框在 `⌘V` 到达时还看不到新内容。
- 针对微信 macOS 输入框延长粘贴消费窗口，再恢复用户原有剪贴板。
- 其他应用仍保持快速粘贴路径。
- 不发送回车，不会自动提交聊天消息。

## 验证

- 全量单元测试通过（97 项）。
- 本版本仍使用“临时剪贴板 + `⌘V` + 条件恢复”的兼容输入链路。
