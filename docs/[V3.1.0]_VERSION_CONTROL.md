# 版本控制文档

## 当前版本：3.3.1

> 版本号以 [main.py](../main.py) 里的 `__version__` 为准，其它地方（README badge 等）都应与之一致。

### 版本更新历史

#### v3.3.1 (2026-07-11 起，持续累积)
**稳定性与数据安全版本**

##### 数据安全
- ✅ **转录缓存不再自我清空**
  - 原子写（tmp + fsync + os.replace），写入被打断不会留下半截文件
  - 读失败绝不返回空字典；损坏文件改名成 `cache.json.corrupt-<时间戳>` 留证
  - 上一代备份 `cache.json.bak`（硬链接，零数据拷贝）
  - 归档迁移改用白名单，只搬音频文件，不再吞掉旁边的备份和工作目录
  - 详见 [转录缓存的数据安全](./TRANSCRIPTION_CACHE_SAFETY.md)

##### 快捷键可靠性
- ✅ **热键光标位移修复** —— 在 Quartz 事件层拦截，`Ctrl+F`/`Ctrl+I` 不再顶走文本光标（[详情](./HOTKEY_CURSOR_FIX.md)）
- ✅ **热键失灵三层自愈** —— event tap 被系统禁用可毫秒级复活，Secure Input 占用会在日志里点名进程（[详情](./HOTKEY_DEAD_TAP_SELFHEAL.md)）
- ✅ **录音状态机修复** —— 只允许从 IDLE 开始录音，消除重触发死循环

##### 转录稳定性
- ✅ **豆包流式断线自动重连**，失败时抢救已识别的文本
- ✅ **豆包握手错误可诊断** —— 报错带 logid 和状态码
- ✅ **大文件转录脚本** —— 支持说话人分离（`test/transcribe_audio_doubao_speaker.py`）

##### 界面与设备
- 🔧 浮动预览窗口在 iTerm2 和跨 Spaces 场景下保持可见
- 🔧 音频设备白名单扩充（Sennheiser ACCENTUM、DJI MIC MINI、本地化的内置麦克风名）

#### v3.3.0 (2026-03-11)
**两遍识别与延迟输出版本**

- ✅ **两遍识别** —— 提高转写准确率
- ✅ **延迟输出** —— 流式结束后一次性输出，可利用完整上下文做优化
- ✅ **DJI Wireless Mic 最高优先级** —— 自动选为输入设备
- 🔧 流式分块从 200ms 降到 100ms，降低延迟

#### v3.2.0 (2026-01-20)
**豆包流式 ASR 与异步队列版本**

- ✅ **豆包流式 ASR** —— 实时转录，成本远低于其它云端方案
- ✅ **浮动预览窗口** —— 转写过程实时可见
- ✅ **异步转录队列** —— 录音一结束就后台转写，不阻塞下一次录音（[详情](./[V3.2.0]_ASYNC_TRANSCRIPTION_QUEUE.md)）
- ✅ **macOS 状态栏指示器** —— 取代往输入框打 `0`/`1` 的临时方案（[详情](./[V3.3.0]_STATUS_BAR.md)）
- ✅ **音频设备自动切换** —— 按优先级白名单选设备，断开时发系统通知（[详情](./device_notification.md)）
- 🔧 本地 whisper 缺失时优雅降级，不再崩溃

#### v3.1.0 (2025-08-13)
**录音安全与文档优化版本**

##### 新增功能
- ✅ **10分钟录音限制**
  - 防止误触导致超长录音消耗API配额
  - 超过10分钟自动中止录音并丢弃音频数据
  - 保护用户API成本安全

- ✅ **状态指示器延迟删除**
  - 添加0.2秒延迟让删除操作更自然
  - 改善用户体验

- ✅ **音频格式转换支持**
  - 测试脚本支持m4a到wav转换
  - 使用FFmpeg进行格式转换
  - 兼容更多音频格式

##### 文档改进
- ✅ **双语文档系统**
  - 英文为默认文档语言
  - 中文文档移至docs/README_zh-CN.md
  - 添加语言切换链接

- ✅ **术语标准化**
  - 统一使用GPT-4o代替GPT-4
  - 保持文档一致性

##### 项目重命名
- ✅ **项目更名为Whisper-Input-Next**
  - 更新所有文档和脚本中的项目名称
  - 保持品牌一致性

#### v3.0.0 (2025-07-27)
**基于原始main分支的功能整合版本**

##### 核心功能整合
- ✅ **OpenAI GPT-4o transcribe 集成**
  - 180秒超时支持长音频
  - 自带高质量标点符号
  - 智能重试循环机制
  
- ✅ **双处理器架构**
  - Ctrl+F: OpenAI GPT-4o transcribe (高质量)
  - Ctrl+I: 本地 whisper.cpp (省钱快速)
  
- ✅ **音频存档系统**
  - 自动保存所有录音到 `audio_archive/`
  - 转录结果缓存 `cache.json` 
  - 支持历史音频重新处理

- ✅ **智能重试机制**
  - 失败时显示 "!" 状态
  - 保存音频支持多次重试
  - 直到成功才结束循环

##### 系统验证
- ✅ **Cache.json 功能验证**
  - 验证音频存档系统正常运行
  - 确认转录缓存机制有效
  - 验证53个音频文件正常存档
  
- ✅ **版本管理系统建立**
  - 实现文档版本标记规范
  - 建立feature版本追踪体系
  - 移除虚假开发计划内容

##### 文档优化
- 🔧 废弃功能标记 (KIMI_USAGE.md → [DEPRECATED])
- 🔧 版本控制文档规范化
- 🔧 README开发状态清理
- 🔧 所有功能文档版本标记统一

---

## 版本规范

### 版本号格式
采用语义化版本控制 (Semantic Versioning): `MAJOR.MINOR.PATCH`

- **MAJOR**: 不兼容的API修改
- **MINOR**: 向下兼容的功能性新增
- **PATCH**: 向下兼容的问题修正

### 版本发布流程
1. 功能开发完成后更新版本号
2. 更新此版本控制文档
3. 更新 `main.py` 中的 `__version__`
4. 创建对应的 Git 标签
5. 更新 README 功能状态

### 下一个版本计划

*当前无计划版本 - 根据需求动态更新*

---

## 开发者注意事项

- 每次功能性commit都应该bump版本号
- 重大更新需要更新版本控制文档
- 保持向下兼容性
- 测试验证后再发布版本