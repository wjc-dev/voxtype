# Voice Input 3.6.1 版本说明

## 修复

- 设置窗口改为菜单栏 App 的辅助窗口，`VoiceInputSettings.appbin` 不再以白色通用图标出现在 Dock。
- 设置窗口不再允许最小化；关闭最后一个设置窗口后，辅助进程会自动退出，避免后台残留。
- 恢复“按住说话，松开结束”和“按一下开始，再按一下结束”两种操作方式。
- 设置页明确提示修改需要“保存并重启”才会生效，并提示 Karabiner 用户按映射后的系统按键配置。
- 新增切换模式回归测试；两种模式继续共用单会话、取消、重复提交和光标写入保护。

## 升级说明

配置、API 凭证和本机纠错数据继续保存在 `~/Library/Application Support/Voice Input Next`，覆盖安装不会删除。当前内测 App 使用 ad-hoc 签名；覆盖新构建后若快捷键或写入失效，请在系统“辅助功能”和“输入监控”中删除旧条目、重新添加当前 `/Applications/Voice Input.app` 并重启。
