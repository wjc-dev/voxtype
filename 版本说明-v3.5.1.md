# Voice Input 3.5.1 版本说明

## 定位

这是仅使用千问 Qwen3 ASR Realtime 的 macOS 系统级语音输入版本。它不是一个输入法，也不会切换搜狗等当前输入源；录音时在光标附近的胶囊里显示累计预览，结束后向录音开始时锁定的输入框提交一次最终文字。

## 重点改进

- 单会话状态机和幂等最终提交：重复回调、迟到回调和取消后的回调都不会造成重复输入。
- 原输入框锁定：录音期间切换 App 或输入框不会向新位置误输入，未写入结果最多私密保留 5 条。
- 自动输入不再依赖剪贴板，也不会发送 Return 或换行；恢复文字只有用户明确点击“复制”时才会进入剪贴板。
- 极短按、静音和上下文回显安全门，避免短按时把 `corpus.text` 作为识别结果写出。
- 优先使用系统默认麦克风和原生 16 kHz 采集；不支持时使用连续重采样，并保留 120 ms 尾音。
- 胶囊累计预览、音量波形、淡入淡出和多屏定位；目标输入框不做抖动式临时改写。
- 右 Option、Fn/Globe、右 Command 和自定义组合键；支持按住说话、切换模式、Esc 取消和事件监听自恢复。
- 默认快捷键后端改为 Quartz 只读监听：回调始终返回原事件，不具备吞键或改键能力，降低企业终端防护误判面。
- 移除运行时 `pynput` 和主动按键拦截实现，安装包不再携带该全局键盘拦截依赖。
- 菜单栏波形左键可直接开始/结束录音，右键打开设置；可完全关闭全局快捷键，用无键盘监听模式工作。
- 新增系统注册型 `⌃⌥Space`：只注册这一组组合键，不建立键盘监听，也不需要输入监控权限，适合公司管控电脑。
- SwiftUI 原生设置、窗口可靠置前、状态栏入口、Finder 再次打开入口、恢复与无内容诊断。
- 共享纠错 2.0：观察输入后的人工替换，相同纠正累计两次才启用；支持查看、撤销和删除，避免连锁与常用短词误替换。
- App 图标改为波形与插入光标，不再使用麦克风加文字的旧设计。

## 设计边界

- 胶囊是流式的，编辑器只接收最终结果。真正带下划线的 marked text 需要注册 InputMethodKit 输入源，会破坏“搜狗保持当前输入法”的核心目标。
- 口语清理默认关闭。“仅去除语气词”是可选的本地确定性处理；广泛去重仍属于保守实验选项，不调用第二个大模型，因此不会阻塞最终提交。
- 当前按 App 写入策略为：ChatGPT 桌面端优先 Unicode 事件，避免 AXValue 引起光标闪到开头；其他可读写输入框优先局部 AX 替换，失败后才在焦点复核后使用 Unicode 事件。

## 千问个性化能力评估

当前模型可靠支持每轮会话的 `corpus.text` 上下文，因此个人词汇和达到频次阈值的纠错对可以作为识别提示发送。它不是服务端永久用户词典：本机仍是唯一事实来源，开关默认谨慎，且所有输出必须经过静音与上下文回显安全门。现阶段不迁移到其他“热词”接口，因为不同 ASR 产品的热词参数并不与 Qwen3 Realtime 等价；在官方接口和真实收益都验证前，不增加不稳定依赖。

## 原生 Swift 迁移评估

3.5.1 采用 Python/PyObjC 运行核心加 SwiftUI 设置页：已有音频、WebSocket、AX 回归测试可复用，风险最低。后续若公开分发，可先把菜单栏、录音和快捷键事件层迁到 Swift，再保留经过验证的文本处理规则；不建议一次性重写网络与纠错层。真正公开发布还需 Apple Developer ID、hardened runtime、公证和 stapling。

## 验证与已知限制

- 自动回归：61 项测试，覆盖重复最终回调、取消、失焦、连续两段空格、UTF-16、重采样、恢复存储、设置窗口启动竞态、只读快捷键不吞键、系统注册键按下/松开、状态栏左右键、纠错阈值和上下文回显。
- 构建目标：Apple Silicon、macOS 13 及以上。
- 小范围包使用 ad-hoc 签名；没有 Developer ID 时，首次打开可能需要在“隐私与安全性”手动确认，公司管控电脑可能仍会拦截。
- 云壳等企业防护是否放行最终由公司的安全策略决定；本版本减少敏感能力并提供无全局监听模式，但不会也不能绕过企业拦截。
- 设置中的最近一轮诊断只记录时长、预览次数、首次预览延迟和是否写入，不记录说话内容、上下文、目标 App 或凭证。

## 回滚

- App 版：退出 Voice Input，将 `/Applications/Voice Input.app` 替换为上一版备份；`~/Library/Application Support/Voice Input Next` 中的配置与学习数据不会被覆盖。
- 源码版：不要 `git reset --hard`。可从稳定检查点新建分支：`git switch -c rollback-v3.4.2 44c65c0`，再重新运行 `install.command`。
