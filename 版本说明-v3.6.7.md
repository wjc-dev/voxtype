# Voice Input v3.6.7

## 修复网页编辑器文字无法立即编辑

- 修复 Qoder、Codex、VS Code 等 Chromium/Electron 编辑器中，语音文字视觉上出现但无法立即删除或编辑的问题。
- 完整值写入后发送一次经过前后校验的编辑器提交脉冲，让网页编辑器同步内部状态。
- 如果空格只负责提交而没有增加字符，程序绝不会继续发送删除键；只有确认确实新增了一个空格时才会删除该空格，避免误删最后一个字。
- 每次按键前重新验证原输入框仍然获得焦点，焦点变化时立即停止。
- 补充识别 Qoder 的“追加需求或提问”占位提示。

## 安全边界

- 不使用剪贴板。
- 不发送回车，不会自动提交聊天消息。
- 不再通过某个字母键承载 Unicode 文本。
