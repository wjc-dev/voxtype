# Voice Input 3.5.2 版本说明

## 本次修复

- 将 App 的长期固定 Bundle ID 更新为 `com.voiceinputnext.qwen`。
- 修复 3.5.0 备份与 3.5.1 使用相同临时签名身份时，macOS“辅助功能”把新 App 合并显示成旧备份的问题。
- 登录项同步迁移到新身份，并清理旧登录项 plist；不会删除 App、AK、个性化上下文或纠错数据。
- 打包结束后自动清理工作目录中的临时 `.app`，避免 LaunchServices 把隐藏的旧构建误认为当前 App，导致辅助功能开关授权到错误版本。
- 保留 3.5.1 的企业安全型快捷键：默认 `Control + Option + Space`，使用系统注册型快捷键，不监控整块键盘。

## 升级与权限

3.5.2 会继续使用本机已有配置目录，因此无需重新填写千问 API Key。由于 App 身份有意更新，首次升级需要在“系统设置 → 隐私与安全性 → 辅助功能”中单独加入并开启新的 `Voice Input`。完成一次后，后续 3.5.x 更新将沿用这个稳定身份。

旧的 `Voice Input v3.5.0 Backup.app` 仍可保留用于回滚，但不要把它误认为当前版本。当前版本可在 App 设置和诊断信息中确认显示为 3.5.2。

## 回滚

退出当前 `Voice Input` 后，可重新打开备份 App。回滚时 macOS 可能要求重新确认备份版的辅助功能权限；本机配置数据不会随 App 切换而删除。
