# Voice Input 3.6.0 版本说明

## 新增

- 增加豆包 Seed ASR 2.0，可在“引擎”页与千问切换。
- 豆包使用流式预览、语义顺滑和二遍识别；仍只向光标提交一次最终文字。
- 增加一次性同音频对比工具；录音只在内存中存在，不写入磁盘。

## 保持不变

- 默认引擎仍是千问；升级不会自动切换现有用户。
- 两个引擎共用快捷键、标点模式、悬浮声波、光标锁定、重复结果防护和本机纠错。
- 点击菜单栏图标只打开设置，不开始录音。
- 正常语音输入不使用剪贴板，录音存档默认关闭。
- App 的 Bundle ID 和本地数据目录保持不变，覆盖更新不会删除 API Key、设置和纠错记录。

## 豆包配置

需要火山引擎语音应用的 App ID 和 Access Token，并开通“豆包流式语音识别模型 2.0 小时版”。每位用户应使用自己的凭证。

## 回滚

退出 Voice Input 后重新安装 3.5.5 即可。3.6.0 新增的豆包凭证字段不会影响旧版读取千问配置。

