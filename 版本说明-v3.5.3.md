# Voice Input 3.5.3 版本说明

## 输入可靠性修复

- 修复中文 Unicode 负载被目标编辑器丢弃时，载体虚拟键码泄漏成单独字母 `a` 的严重问题。
- 默认彻底禁用合成 Unicode 键盘回退，不再向当前输入法发送虚拟按键。
- 最终文字依次使用 AX 选区写入和 AXValue 安全写入；两种方式都失败时保留到恢复记录，不向输入框写入错误字符。
- 针对 Codex/ChatGPT 等 OpenAI 桌面编辑器优先使用 AXValue，并在写入后验证实际内容。
- 写入失败日志只记录目标角色、可写属性和进程号，不记录转写正文。

## 菜单栏交互修复

- 左键点击菜单栏图标改为打开并置前设置窗口，不再开始或停止录音。
- 右键点击同样可恢复/置前设置窗口。
- 录音只由设置中选定的快捷键触发；默认是 `Control + Option + Space`。

## 已知安装边界

没有 Developer ID 时 App 使用 ad-hoc 签名。更新 App 本体后 macOS 可能要求重新确认一次辅助功能权限；3.5.2 已修复旧构建 App 污染 LaunchServices 的问题，系统现在只会解析 `/Applications/Voice Input.app`。
