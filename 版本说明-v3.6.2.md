# Voice Input 3.6.2 版本说明

## 修复

- 单修饰键快捷键（右 Option、右 Command、Fn）改用 AppKit 的只读全局事件监视器，不再创建会被企业安全软件每 5 秒停用的原始 `CGEventTap`。
- 保持按键事件原样传递，Voice Input 无法吞掉或改写用户键盘输入。
- 组合键继续使用 Carbon 系统注册机制，不需要输入监控权限。
- 新增右 Option 按下、松开及只读监听回归测试；完整测试共 74 项。

## 权限说明

右 Option、右 Command、Fn 等单修饰键仍需要 macOS 的“输入监控”授权；普通组合键不需要。当前内测版使用 ad-hoc 签名，覆盖更新后 macOS 可能要求重新确认一次权限。
